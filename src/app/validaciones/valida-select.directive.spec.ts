import { ValidaSelectDirective } from './valida-select.directive';

describe('ValidaSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaSelectDirective();
    expect(directive).toBeTruthy();
  });
});
